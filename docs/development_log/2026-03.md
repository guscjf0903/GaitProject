# 개발 일지 (2026-03)

> Git 기반 AI 채팅 서비스(Gait Project) 개발 과정을 기록한 문서입니다.  
> 작성 규칙: 이 파일을 복사해서 `YYYY-MM.md`로 저장 후, 날짜별로 누적합니다.

---

## 이번 달 목표

- **데이터 무결성 강화**: 프론트엔드에 위임되었던 DB 저장 책임을 백엔드로 이관.
- **토큰 과금 시스템 기반 마련**: AI와 주고받은 실제 프롬프트(`rawPrompt`)와 응답(`rawResponse`), 그리고 정확한 토큰 사용량을 로깅.
- **AI 요약 자동화 안정화**: 커밋 시 과거 대화 내역 기반 Long Summary 자동 생성 파이프라인 검증.

---

## 2026-03-01

### 한 일

- **데이터베이스 스키마 확장**
  - `Message` 엔티티에 `rawPrompt`, `rawResponse`, `inputTokens`, `outputTokens`, `totalTokens` 컬럼 추가.
  - Hibernate `ddl-auto=update`를 통해 DB 테이블 자동 반영.
  
- **백엔드 주도 메시지 저장 리팩토링 (Architecture Update)**
  - 기존: 프론트엔드가 사용자의 입력을 화면에 띄운 후 `MessagesService.send`를 호출하고, SSE가 끝나면 또 호출하여 저장하는 구조.
  - 변경: 프론트엔드는 렌더링과 스트리밍 요청(`POST /api/chat/stream`)만 담당. 
  - `ChatStreamController`가 요청을 받자마자 조립된 컨텍스트(`rawPrompt`)를 포함하여 USER 메시지를 직접 저장하고, 스트리밍 종료 후 토큰량과 함께 ASSISTANT 메시지를 직접 DB에 저장하도록 책임을 완전히 분리함.

- **Spring AI 토큰 메타데이터 로깅 연동**
  - `OpenAiChatAiService`에 `OpenAiChatOptions`를 명시적으로 주입하여 OpenAI API에 `streamUsage(true)`(또는 `stream_options: {"include_usage": true}`) 옵션을 활성화.
  - 스트리밍의 마지막 청크에 담겨오는 Usage 객체를 파싱하여 정확한 토큰 사용량을 산출.

- **프론트엔드 토큰 알림 UI 연동**
  - `sseChatStream.ts`에서 `ANSWER_DONE` 이벤트 발생 시 서버가 넘겨준 JSON 메타데이터를 파싱하도록 수정.
  - `ChatPage.vue`에서 스트리밍 종료 후 "이번 대화: OOO tokens 사용" 이라는 토스트 알림 노출.

### 결정/이유

- **왜 백엔드가 직접 저장하도록 변경했는가?**
  - 기존 방식은 백엔드에서 생성된 중요한 데이터(동적 조립된 `rawPrompt`, 실제 사용된 Token 수)를 DB에 넣기 위해, 프론트엔드로 데이터를 다시 내려보낸 뒤 프론트엔드가 이를 받아 API로 저장 요청을 해야 하는 매우 기형적이고 불안정한 구조였음.
  - 시스템 프롬프트 조립과 보안/과금 처리는 전적으로 백엔드의 책임이므로, 컨트롤러 레벨에서 즉시 저장하도록 리팩토링하여 데이터 정합성과 코드의 책임 소재를 명확히 함.

### 이슈/막힌 점

- **프론트엔드 OpenAPI 클라이언트 타입 불일치**: 백엔드 API 명세가 변경되어 `npm run generate:api`를 수행했으나, 자동 생성 과정에서 `list4` -> `list` 등으로 함수명이 변경되어 화면 렌더링 에러 발생. (수동으로 함수명 매핑하여 해결)
- **Spring AI의 EmptyUsage 이슈**: 기본적으로 Spring AI는 스트리밍 시 토큰 사용량을 내려주지 않아 `NULL`로 기록됨. `SPRING_AI_OPENAI_CHAT_OPTIONS_STREAM_USAGE=true` 설정 및 코드 레벨의 빌더 주입을 통해 해결.
- **환경변수 개행 오류로 인한 AI 실패**: `.env.local` 수정 중 줄바꿈이 누락되어 모델명이 `gpt-4o-miniSPRING...`으로 합쳐져 OpenAI API에서 `HTTP 400 (Invalid Model ID)` 에러 발생 및 연쇄적으로 Long Summary가 생성되지 않는 버그 발생. (환경변수 분리하여 해결)
- (상세한 트러블슈팅 과정은 `docs/troubleshooting/2026-03.md` 참조)

### 다음 할 일

- 사용자 계정별로 부여된 토큰 예산에서 실제 사용된 토큰(totalTokens)을 차감하는 과금/크레딧 로직 연동 (`TokenBillingService`).
- 프론트엔드 좌측 하단의 "1.2M Tokens left" UI를 실제 DB와 연동.
- RAG 파이프라인 (pgvector) 임베딩 로직 고도화.

---

## 2026-03-15

### 한 일

- **백엔드: 머지 컨텍스트 조립 유틸 분리**
  - 신규 `MergeContextBuilder.kt` 추가. `MergeService`에 있던 공통 조상 탐색·경로 수집·재료 추출을 분리.
  - 데이터 구조: `MergeBudget`(totalInputTokens, rawMessagePercent, shortSummaryPercent, longSummaryPercent) — `SQUASH` 2000토큰(20/50/30), `DEEP` 6000토큰(40/20/40). `BranchMaterial`(branchName, rawMessageExcerpt, shortSummaries, longSummaries). `MergeContext`(fromMaterial, toMaterial, commonAncestor, fromPath, toPath).
  - `MessageRepository.findByWorkspace_IdAndCommit_IdInAndDeletedAtIsNull`로 경로 내 커밋에 귀속된 메시지 조회 후, `TokenEstimator.estimate`로 토큰 산정·`trimToBudget`(최신 우선, 예산 초과 시 잘라냄) 적용해 재료 문자열 생성.
  - `build( fromBranch, toBranch, workspaceId, mergeType ): MergeContext` — to 조상 Set 구성 후 from을 역순으로 올라가며 공통 조상 확정, fromPath/toPath 역순 정렬 후 `buildBranchMaterial` 호출.

- **백엔드: MergeSummaryAiService 이중 모드**
  - `MergeSummaryAiService.kt`: 기존 단일 `summarizeMerge` 대신 `summarizeSquash( context: MergeContext, userNote )`, `summarizeDeep( context, userNote )` 추가.
  - Squash: `@Value("app.ai.merge-summary.squash-model")` 기본 `gpt-4o-mini`, `maxTokens(800)`, `temperature(0.3)`, `buildSquashPrompt`(간단 규칙·JSON 스키마). Deep: `deep-model` 기본 `gpt-4o`, `maxTokens(2000)`, `temperature(0.4)`, `buildDeepPrompt`(타임라인/겹침/보강/충돌/최종결론 섹션 지시).
  - 공통: `appendBranchMaterial`로 from/to의 short/long/raw 발췌를 프롬프트에 붙임. `callAi( prompt, options, mode )`에서 `ResponseFormat.Type.JSON_OBJECT`로 호출 후 `ObjectMapper.readValue( json, AiMergeSummary::class.java )`, 예외 시 fallback `AiMergeSummary` 반환.

- **백엔드: MergeService 정책 및 플로우 리팩토링**
  - `MergeService.kt`: `FAST_FORWARD` 분기 전체 삭제. `reqMergeType` 기본값 `MergeType.SQUASH`. `validateMergePolicy( userPlan, mergeType )` — FREE면 예외, DEEP이면 STANDARD 예외, FAST_FORWARD/NONE 요청 시 예외.
  - 플로우: `mergeContextBuilder.build(...)` → fromPath 비어 있으면 "이미 최신 상태" 예외. SQUASH/DEEP 공통으로 `mergeSummaryAiService.summarizeSquash` 또는 `summarizeDeep` 호출 후 `Commit` 생성(parent=toBranch.headCommit, mergeParent=fromBranch.headCommit, isMerge=true, mergeType, keyPoint/shortSummary/longSummary). AI 실패 시 `buildFallbackShortSummary`/`buildFallbackLongSummary`로 context 재료 기반 문자열 생성.
  - `MergeContextBuilder` 의존성 주입 추가.

- **백엔드: Merge DTO 확장**
  - `MergeDtos.kt`: `MergeResponse`에 `fromBranchName: String?`, `toBranchName: String?` 추가. `fromEntity( entity, fromBranchName?, toBranchName? )` 시그니처 변경(기본값 null이면 entity.fromBranch.name 등 사용). `MergeService`에서 `MergeResponse.fromEntity( saved, fromBranch.name, toBranch.name )` 호출.

- **프론트엔드: 커밋·타임라인 타입 및 데이터 확장**
  - `ChatPage.vue`: `CommitNode` 타입 정의 — hash, parentId, mergeParentId?, isMerge?, mergeType?, branch, branchId?, msg, time, col, shortSummary?, longSummary?. `commits` ref와 `commitByHashMap`을 해당 타입으로 통일.
  - `bootstrapFromServer` 내 커밋 매핑에서 `mergeParentId`, `isMerge`, `mergeType`, `shortSummary`, `longSummary`를 API 응답 필드와 매핑.
  - `ChatMsg` 유니온에 `{ type: 'merge'; hash; text; mergeType?; shortSummary?; longSummary?; expanded? }` 추가.

- **프론트엔드: 그래프 merge 엣지·노드**
  - `graphPaths` computed: 기존 parent 엣지에 `isMergeEdge: false` 추가. `commits.value` 순회로 `mergeParentId`가 있는 커밋에 대해 동일 곡선 공식으로 path 생성 후 `paths.push(..., isMergeEdge: true )`. 타입을 `Array<{ d, color, isActive, isMergeEdge }>`로 변경.
  - SVG `<path>`에 `:stroke-dasharray="path.isMergeEdge ? '6 4' : 'none'"`, merge 엣지는 stroke-width 1.5·opacity 0.45.
  - `graphNodes`에 `isMerge: c.isMerge === true` 추가. 노드 렌더링 분리: 일반 노드는 `graphNodes.filter(n => !n.isMerge)`로 `<circle>`, merge 노드는 `filter(n => n.isMerge)`로 `<rect>` + `transform="rotate(45 ...)"` 다이아몬드.

- **프론트엔드: 타임라인 merge 카드**
  - `buildTimelineRows`: 메시지를 `commitId` 기준으로 `messagesByCommit` Map 구성. `currentHead`부터 parent 역순으로 리니지 배열 `lineage` 생성. lineage(및 메시지는 있으나 리니지에 없는 commitId) 순으로 순회하며, `commitByHashMap`에서 커밋 조회. `isMerge`면 `{ type: 'merge', hash, text, mergeType, shortSummary, longSummary }` 행 추가; 아니면 메시지가 있을 때만 기존 commit divider 추가. 그 다음 해당 커밋의 메시지들 행 추가. pending은 기존처럼 WORKING_TREE divider + 메시지.
  - `isFutureMessage`: `msg.type === 'commit' || msg.type === 'merge'` 통합 처리. `msg.type === 'message'`인 경우만 commitId 인덱스 비교.
  - 템플릿에 `v-else-if="msg.type === 'merge'"` 블록: 점선 구분선, 카드 내 mergeType 뱃지(Squash/Deep), keyPoint, shortSummary, longSummary 펼치기 버튼 및 `msg.expanded`로 접기/펼치기.

- **프론트엔드: 머지 모달**
  - `mergeType` 옵션에서 `FAST_FORWARD` 제거. `MergesService.create1` 호출 시 `mergeType as 'SQUASH' | 'DEEP'`.
  - Squash/Deep 선택 시 설명 문단 표시. notes textarea를 DEEP 전용이 아닌 공통으로 변경.
  - `appendCommitToGraph` 호출 시 새 커밋 객체에 `mergeParentId: null`, `isMerge: false`, `mergeType`, `shortSummary`, `longSummary` 필드 명시.

### 결정/이유

- **FAST_FORWARD 제거**: 서비스 코드에서만 분기 제거. DB enum `merge_type`에는 `FAST_FORWARD` 유지(기존 merges 레코드 호환). 요청 시 `validateMergePolicy`에서 "지원되지 않음" 예외.
- **토큰 예산 고정 비율**: Squash는 입력 2000·출력 800, Deep은 6000·2000으로 상한을 두고, 재료는 원본/short/long 비율로 잘라서 AI 입력 크기 제어 및 비용 예측 가능하게 함.
- **타임라인 리니지 기반**: 메시지만으로 행을 만들면 merge 커밋(메시지 없음)이 빠지므로, `currentHead`→parent 체인을 먼저 만들고 그 순서대로 커밋 구분자/merge 카드/메시지를 삽입해 merge가 타임라인에 항상 위치하도록 함.

### 이슈/막힌 점

- **프론트 paths 타입**: `graphPaths`에 merge 엣지를 넣으면서 `isMergeEdge` 필드를 추가했으나, 기존 타입 `{ d, color, isActive }`에 없어 린트 에러. paths 배열 타입에 `isMergeEdge: boolean` 추가하여 해결. (상세: `docs/troubleshooting/2026-03.md` TS-009)

### 다음 할 일

- 머지 후 타겟 브랜치로 이동 시 merge 커밋 자동 포커스/스크롤(선택).
- 토큰 과금·RAG 파이프라인 등 기존 다음 할 일 유지.

---
